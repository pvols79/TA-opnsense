# TA-opnsense transforms placeholders.
# Implement routing + extraction logic with minimal, readable regex.

[opnsense_route_filterlog]
# Example sourcetype routing stanza.
# REGEX = filterlog:
# DEST_KEY = MetaData:Sourcetype
# FORMAT = sourcetype::opnsense:filterlog

[opnsense_route_unbound]
# Example sourcetype routing for unbound.
# REGEX = unbound\[[0-9]+\]:
# DEST_KEY = MetaData:Sourcetype
# FORMAT = sourcetype::opnsense:unbound

[opnsense_route_openvpn]
# Example sourcetype routing for OpenVPN logs.
# REGEX = openvpn\[[0-9]+\]:
# DEST_KEY = MetaData:Sourcetype
# FORMAT = sourcetype::opnsense:openvpn

[opnsense_route_fallback]
# Fallback routing when no parser matches.
# REGEX = .
# DEST_KEY = MetaData:Sourcetype
# FORMAT = sourcetype::opnsense:undefined

[opnsense_extract_common]
# Example extraction block for common key/value logs.
# REGEX = (?:^|,|\s)([A-Za-z_][A-Za-z0-9_]*)=([^,\s]+)
# FORMAT = $1::$2
# MV_ADD = true
