# TA-opnsense sourcetype parsing placeholders.
# Add parsing and field extraction logic incrementally, with examples.

[opnsense:filterlog]
# Primary firewall filter log sourcetype.
# Suggested future keys:
# SHOULD_LINEMERGE = false
# TIME_PREFIX = ^\w{3}\s+\d+\s+\d\d:\d\d:\d\d
# REPORT-opnsense-filterlog = opnsense_filterlog_extract

[opnsense:unbound]
# Unbound DNS resolver logs.
# REPORT-opnsense-unbound = opnsense_unbound_extract

[opnsense:openvpn]
# OpenVPN service logs.
# REPORT-opnsense-openvpn = opnsense_openvpn_extract

[opnsense:cron]
# Cron daemon logs from OPNsense.
# REPORT-opnsense-cron = opnsense_cron_extract

[opnsense:dhclient]
# DHCP client lifecycle logs.
# REPORT-opnsense-dhclient = opnsense_dhclient_extract

[opnsense:firewall]
# Generic firewall subsystem logs when not emitted as filterlog.
# REPORT-opnsense-firewall = opnsense_firewall_extract

[opnsense:lighttpd]
# lighttpd web service logs.
# REPORT-opnsense-lighttpd = opnsense_lighttpd_extract

[opnsense:dhcpd]
# DHCP server daemon logs.
# REPORT-opnsense-dhcpd = opnsense_dhcpd_extract

[opnsense:configctl]
# configctl command execution logs.
# REPORT-opnsense-configctl = opnsense_configctl_extract

[opnsense:configd.py]
# Python configd orchestration logs.
# REPORT-opnsense-configd_py = opnsense_configd_py_extract

[opnsense:undefined]
# Catch-all fallback sourcetype for uncategorized OPNsense events.
# TRANSFORMS-opnsense-undefined = opnsense_route_fallback
