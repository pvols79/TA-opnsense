# Splunk app manifest for TA-opnsense.
# Keep this file minimal and AppInspect-friendly.

[install]
# Splunk marks whether setup has completed.
is_configured = 0

[ui]
# Keep TA hidden from the Splunk app launcher.
is_visible = false
label = TA-opnsense

[launcher]
# Semantic version for this TA release.
version = 0.1.0

description = Technical Add-on for normalizing OPNsense logs into CIM-aligned fields.

[package]
# App/package ID used by packaging tooling.
id = TA-opnsense
