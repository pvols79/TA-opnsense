# Optional eventtypes for CIM tagging and acceleration scopes.

[opnsense_network]
# search = sourcetype=opnsense:filterlog OR sourcetype=opnsense:firewall

[opnsense_vpn]
# search = sourcetype=opnsense:openvpn
